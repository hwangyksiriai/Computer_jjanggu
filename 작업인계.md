# 회사 PC에서 이어서 작업하기

## 최신 반영 · 2026.09.23.6

사용자의 요청에 따라 파일 고정·사진 찜 영구 보관에 이어 자동 모음 → 비교 보강 → 메일 첨부파일 연결을 구현했다. 현재 PC의 `outputs/짱구주머니-목소리적용/짱구주머니.exe`를 업데이트하고 재실행 응답, 기존 바로가기, 목소리·검색 위치·정리 설정 보존을 확인했다. 실행 파일 SHA-256은 `a0bf5473819587a5bf00c43bb70c24144e5563c6335f671d39e67d22e6e0c074`다. 이전 `.5` ZIP/설명서는 그대로 보존하며, GitHub 배포 태그는 `v2026.09.23.6`이다. 소스와 별도로 릴리스에 ZIP 실행판·HTML/TXT 사용설명서·공개 체크섬을 제공한다.

자동 모음은 `smart_collections.py`/`smart_collections_ui.py`와 별도 SQLite에 이름·검색어·문서 종류·파일 수정일 조건을 저장한다. 읽기 전용 색인 조회, 현재 문서 검색 범위, 취소·세대 확인으로 새 파일을 갱신한다. 일반 검색으로 전환한 결과창을 덮어쓰지 않으며, 재색인 전 원본의 mtime/size가 달라진 파일은 오래된 본문으로 일치시키지 않는다. 날짜는 발행일/급여월이 아니라 파일 수정일이다.

`document_diff.py`는 현재 원본의 TXT계열·PDF·DOCX 글자를 제한된 작업자로 읽어 추가/삭제를 보여준다. 스캔·읽기 실패는 판단 불가, 일부 범위 비교는 범위를 명시한다. `final_versions.py`는 사용자 지정 최종본과 stat identity를 저장한다. 수정됨/원본 없음 안내, 무관 문서 표시 보존, 정리/되돌리기 remap을 연결했다.

`mail_*.py`는 네이버·네이버웍스·Gmail 앱 비밀번호와 회사 IMAP SSL의 받은편지함 읽기 전용 가져오기 및 사용자가 선택한 `.eml` 파일 가져오기를 지원한다. 최근 90일/최신 100통, 메일 25MB·첨부 15MB, 계정 250MB/2천 개의 한도가 있다. 자동 주기 동기화는 없고 가져오기 버튼으로 갱신한다. Outlook/Microsoft 365는 `.eml`만 지원하며 OAuth 연결은 미구현이다. 서버 메일·읽음 상태 변경 및 발송 기능은 없다. 비밀번호는 기본 저장하지 않으며 선택 시 Windows DPAPI 사용. 실제 계정에 접속하지 않았고 실제 서버 호환성은 사용자 연결 후 확인해야 한다. 자세한 인증 조건·공식 문서 링크는 `MAIL_CONNECTIONS.md`.

메일 사본 폴더만 문서 검색 범위에 추가하며 오프라인 기존 폴더 선택도 보존한다. 계정 DB/자격증명 파일은 색인 범위에 넣지 않는다. 출처는 단건 SQL로 조회해 접힌 파일 정보에 보낸 사람·제목·메일에 적힌 날짜를 표시한다. `.eml` 원본 열기와 IMAP 메일함 열기를 구분한다. 문서 검색 범위 변경은 별도의 사진 갤러리를 비우지 않는다.

검증: 소스 통합 및 완성된 실행 파일 자체 검사 각각 76개 통과. 최종 자동 모음 backend16/Tk8, 비교 backend42/Tk22, 메일 backend26/Tk6 및 실제 DPAPI 합성 문자열 왕복, 결과창27/파일드래그24, 교차 기능 회귀4개를 확인했다. 합성 문서의 실제 비교 화면에서 최종본 표시를 클릭하고 자동 모음 화면도 확인했다. 사용자 메일·클립보드·원본 문서로 검사하지 않았다. `verify_next_workflows.py`는 `.eml`→본문 검색→출처→새 파일 자동 모음→범위 변경→글자 비교/최종본을 연결해 검사한다.

## 이전 중간 점검 기록 · .5 설치판 제공 당시

사용자가 새 기능 구현 전 버전과 예정 기능을 포함한 설명서를 먼저 요청해 당시 설치판 2026.09.23.5를 유지하고, `outputs/짱구주머니-2026.09.23.5-받기`에 이전 실행 파일과 필요한 `_internal`만 넣은 ZIP 및 TXT/HTML 설명서를 생성했다. ZIP의 CRC와 실행 파일 SHA-256을 기존 검증판과 대조했다. 개인 설정·DB·등록 음성은 포함하지 않았다. 이 파일은 새 `.6` 설치판과 별도로 보존한다.

새 소스의 저장소 테스트 24개와 결과창 테스트 24개, 통합 검사 67개는 통과했다. 통합 검사 첫 실행에서 간헐적인 Tk 콜백 인자 오류가 1회 발생했고 traceback 수집을 보강한 재실행에서는 재현되지 않았다. 해결로 단정하지 말고 다음 실행판 빌드 전에 확인할 것. 사진 갤러리·큰 보기의 저장 원본 재검증 관련 GUI 회귀도 마무리 확인한 뒤 새 버전을 빌드한다.

다음 개발 단계인 자주 쓰는 파일 고정과 사진 찜 영구 보관을 구현했다. `file_memory.sqlite3`에 pinned_files/saved_photos 테이블을 추가하며 기존 최근·모음 데이터는 보존한다. 고정·찜은 경로 참조와 제한된 표시 정보만 저장하고 원본은 이동·복사·삭제하지 않는다. 앱의 정리/되돌리기 경로 remap에도 함께 반영한다.

말풍선 결과의 ☆, 큰 결과창의 ‘☆ 고정하기’로 고정한다. 말풍선 첫 화면은 사용 가능한 고정 파일 최대 3개를 우선 표시하며 없으면 최근 파일 3개를 보여준다. ‘모두 보기’ 또는 ‘내 모음 → ★ 고정한 파일’에서 누락된 경로까지 관리한다. 원본이 없어도 고정 해제는 가능하고, 같은 경로의 버튼 상태는 열린 창들에 함께 반영한다.

사진의 ♡는 앱 재시작 뒤에도 유지한다. ‘내 모음 → ♥ 찜한 사진’은 현재 문서 검색을 바꾸지 않고 저장한 사진만 연다. 누락되거나 다른 파일로 바뀐 경로는 예전 썸네일을 재사용하지 않으며 찜 해제할 수 있다. 확인된 휴지통 이동이 성공했을 때만 영구 찜에서도 제거한다. 사진 삭제 실패·취소는 찜을 유지한다.

## 최신 반영 · 2026.09.23.5

첫 남은 개발 단계인 검색 결과 파일 복사·외부 첨부용 드래그를 구현했다. `EasyApp.copy_result_files`를 통해 말풍선의 복사, 큰 결과창의 파일 복사, 최근 파일·내 모음과 기본 결과 카드에서 같은 Windows 파일 클립보드를 쓴다. 파일 이름 버튼의 Ctrl+C만 파일 복사로 연결하여 검색칸·본문의 문자 복사는 유지한다.

`file_transfer.py`는 동작 직전 정규 파일·전체 경로를 검증하고 tkinterdnd2에 COPY만 허용한다. 실제 드래그에서는 파일 열기 버튼의 mouseup command를 억제한다. 누락된 파일은 조용히 제외하지 않고 전체 작업을 거부한다. `windows_features.copy_files`는 Unicode CF_HDROP와 Preferred DropEffect COPY를 함께 제공하고 네이티브 메모리 소유권을 관리한다. 두 기능 모두 원본 이동·삭제를 하지 않으며, 받는 앱의 첨부 수락·메시지 전송까지 완료했다고 주장하지 않는다.

UI·클립보드 검증에는 합성 파일과 mock Win32를 사용한다. 사용자 클립보드나 실제 메일·메신저에 검증 파일을 넣지 않는다. 이 PC의 폴더형 실행판만 갱신하며 공개 GitHub 릴리스는 별도 요청 시 게시한다.

검증 완료: 클립보드 19개·드래그 24개·결과창 20개 테스트, 실행 파일 자체 검사 55개 통과. 합성 파일을 큰 결과창/말풍선에서 로컬 수신 영역으로 실제 끌어 놓아 COPY 전달과 원본 bytes/mtime 유지, 드래그 후 복사 및 일반 클릭을 확인했다. 드래그 release의 중복 ButtonLeave/Up 호출을 제거하여 다른 버튼의 hover 상태를 훼손하지 않는다. 현재 PC 실행판은 2026.09.23.5이며 기존 바로가기·목소리·검색 위치·정리 설정 유지 및 재실행 응답을 확인했다.

## 최신 반영 · 2026.09.23.4

검색 결과창에 선택형 동일 파일 묶기(`result_grouping.py`, `file_comparison.py`)와 나란히 비교(`file_compare_ui.py`)를 추가했다. SHA-256과 검사 전후 stat으로 현재 목록의 바이트가 같은 파일만 묶는다. 실패·변경·취소는 동일 판정에 포함하지 않는다. 원본 파일을 복사·이동·삭제하지 않는다.

버전 후보는 명시적인 파일명 꼬리(v1/v2/최종/수정본 등)를 보수적으로 비교한다. 급여월·날짜·거래처는 제거하지 않으며 ‘최종본’ 확정 기능이 아니다. 비교창의 파일 열기는 공통 open_file을 사용해 최근 목록에 반영된다. 창을 닫을 때 Tk 변수·콜백을 메인 스레드에서 해제하여 백그라운드 GC 오류를 방지한다.

## 최신 반영 · 2026.09.23.3

문서 검색 위치(`document_roots`)를 정리 위치(`source`/`vault`)와 분리했다. 기존 설정은 처음 한 번 source/demo+vault로 마이그레이션한다. 빈 목록은 검색할 곳 없음이며 전체 DB 검색을 뜻하지 않는다. `찾을 폴더` 창에서 바탕화면·다운로드·문서·OneDrive 또는 사용자 선택 폴더를 연결한다. 경로 선택만으로 정리 설정이나 자동 정리 상태를 바꾸지 않는다.

`file_memory.py`는 앱 데이터 폴더의 `file-memory.sqlite3`에 최근 열린 파일과 이름 있는 모음을 저장한다. 최근 기록은 Windows 원본 열기 요청이 성공한 경우에만 추가하고 최대 100개를 유지한다. 말풍선에는 3개, 최근 파일 창에는 20개를 표시한다. 모음은 파일 참조만 저장하며 모음 제거는 원본에 영향을 주지 않는다. 앱의 move/undo 성공 경로는 최근·모음에도 반영된다. 외부에서 옮기거나 삭제한 파일은 모음에서 누락 상태로 남긴다.

주요 추가 파일은 `document_locations.py`, `document_locations_ui.py`, `file_memory.py`, `collections_ui.py`다. UI 검증은 합성 파일만 사용하는 `verify_easy.py`에 포함한다. 이 버전의 실행판은 현재 PC `outputs/짱구주머니-목소리적용`의 exe와 `_internal` 폴더이며, 기존 바로가기 및 `.local` 음성·모델·정리 설정을 유지한다. 공개 릴리스 링크는 아직 2026.09.22.8이다.

## 최신 반영 · 2026.09.23.1

현재 PC에서 사용자 제공 영상의 참고 음성을 등록하고 Qwen 로컬 합성을 활성화했다. 실제 클릭·검색·결과·결과 없음 문장의 내용/숫자 검사를 통과한 음성을 캐시했다. 상세 결과와 한계는 [VOICE-TEST.md](VOICE-TEST.md)의 2026-09-23 항목을 우선 읽는다.

`.local/settings.json`의 `voice_reference`, `.local/runtime.json`의 `runtime_root`가 현재 PC 연결 기준이다. 이전 D: 경로를 덮어쓰지 말고 현재 파일을 확인한다. 실제 참고 파일은 `.local/voice-reference/shinchan-reference.wav`와 같은 이름의 JSON 대사이며, 검증된 캐시는 `.local/voice-generated`에 있다. 개인 음성과 모델은 Git에 포함하지 않는다.

새 PC에서는 짱구 우클릭 → **목소리 설정**에서 엔진 준비, 참고 음성·대사 연결, 새 답변 들어보기 순서로 준비한다. 독립 Python 설치와 Windows 긴 경로 문제를 수정했으며, 음성 인식만 준비된 PC의 참고 대사 읽기 및 검사 중 취소도 지원한다. 새 문장은 CPU에서 수십 초 걸릴 수 있으므로 자주 쓰는 안내는 검증 후 캐시한다.

## 최신 반영 · 2026.09.22.8

사진 검색의 독립 목록·기존 분석 재사용, 사진 카드와 큰 보기, 찜, 후속 조건 검색, 확인 후 Windows 휴지통 삭제까지 구현했습니다. 사용 순서는 [사진 갤러리 사용법](사진-갤러리-사용법.md), 실행 파일은 [GitHub 배포 페이지](https://github.com/hwangyksiriai/Computer_jjanggu/releases/tag/v2026.09.22.8)에 있습니다.

회사 PC에서 `git pull --ff-only`로 코드를 받은 뒤 해당 PC의 폴더와 로컬 AI 환경을 연결하세요. 실행판의 설정·검색 DB는 `%LOCALAPPDATA%/JjangguPocket`, 소스 실행판은 `.local`을 사용합니다. 사진 검색·삭제 관련 검사 70개와 완성된 실행 파일의 통합 검사 31개를 통과했습니다. 실제 다른 PC에서의 호환성 검증과 아래 음성 품질 작업은 별도로 남아 있습니다.

## 남은 음성 목표

짱구 데스크톱 파일 정리 앱의 **요청 문장에 맞는 짱구 음성 자유 발화**를 완성한다. 파일/음성을 외부 분석 서비스로 보내지 않고 로컬 처리한다. 실패한 합성 음성을 완료품처럼 적용하지 않는다. 원본 인사 녹음을 모든 요청에 반복 재생하지 않는다.

## 옮길 것

- 프로젝트 폴더의 소스, assets, 테스트, 문서. 현재 체험판 ZIP은 이후의 음성 개발 파일을 포함하지 않아 개발 인계용으로 충분하지 않다.
- 음성 작업 재현에 `.local/voice-reference/new-reference.wav` 및 `.local/voice-clips`의 등록 원본이 필요하다. `.local/voice-reference`에 이전 실패 시험본과 검증 보고서도 있다.
- 별도 런타임 폴더에는 검색용 vendor/node_modules/models와 음성용 voice-env/xtts-v2/qwen-packages/qwen3-tts-base가 있다. 가상환경은 복사만으로 동작을 보장하지 않으므로 회사 PC에서 재설치할 수 있다.
- `.local/runtime.json`은 PC마다 다른 절대 경로다. 회사 PC의 실제 설치 경로로 고쳐야 한다. `.local/settings.json`에도 source/vault/voice_clips 절대 경로가 있다. 다른 PC 경로로 정리 작업을 실행하지 말고 새 PC에서 연결 폴더를 재선택한다.
- `.local/library.sqlite3`에는 개인 문서 본문이 들어 있다. 개발 소스와는 별도 데이터다. 원본 문서도 옮기지 않으면 기존 검색 경로는 작동하지 않으므로 회사 PC에서는 해당 폴더를 다시 색인한다. 공개 배포 ZIP과 개인 작업 이전은 구분한다.

## 회사 PC에서 Codex 시작

1. 복사한 프로젝트 폴더를 Codex 로컬 프로젝트로 연다.
2. 이 파일과 VOICE-TEST.md, IMPLEMENTATION-STATUS.md를 읽게 한다.
3. Python 3.11+ 및 Node 22+를 준비한다. 집 PC에서 검증한 기본 앱은 Python 3.14 / Node 24이며 음성 전용 환경은 Python 3.12다.
4. `처음설치.bat`은 앱 전용 .venv를 준비한다. `AI설치.bat`은 검색·대화·음성 인식 모델용이며 **새 Qwen 음성 합성 설치까지 처리하지 않는다**.
5. 앱 실행 전 새 PC 경로를 확인하고 샘플 문서로 테스트한다.

보낼 요청 예시:

> 작업인계.md와 VOICE-TEST.md를 읽고 현재 상태를 확인해. 회사 PC의 런타임 경로부터 맞춰 주고, Qwen 기반 짱구 자유 발화를 검증해서 앱에 연결해. 검증되지 않은 음성이나 인사 녹음 반복을 완료라고 처리하지 마.

## 이미 구현한 것

- 로컬 본문/OCR/의미/이미지 검색, 파일 안전 이동과 복원, 캐릭터 반응, 듀얼 모니터, 머리 위 말풍선.
- 말풍선 결과 조건 좁히기와 별도 넓은 목록/오른쪽 미리보기 (`result_refinement.py`, `result_browser.py`).
- 급여 문서 검색 오탐 제한: 급여 단서 없는 일반 문서는 임베딩 점수만으로 포함하지 않는다.
- 단위/회귀 43개 및 verify_bubble.py 실제 Tk 흐름 통과(음성 새 엔진 이전 기준).
- 원본 음성은 반응별 클립 방식. 현재 설정 original_only에서는 해당 반응 클립이 없으면 텍스트 안내, 일반 TTS나 엉뚱한 인사로 대체하지 않는다.

## 음성 작업 진행 중 — 완료 아님

- XTTS는 참고 영상 두 개, 구간/생성 설정 세 가지를 시험했지만 문장 정확성 검증 실패. `compare_voice.py`, `verify_voice_comparison.py`, VOICE-TEST.md 참조. 같은 시험만 반복하지 않는다.
- 새 엔진: 공식 `Qwen/Qwen3-TTS-12Hz-0.6B-Base`, 고정 revision `5d83992436eae1d760afd27aff78a71d676296fc`.
- `setup_qwen_voice.py`는 위 모델을 런타임/qwen3-tts-base에 다운로드하고 파일 크기와 제공된 SHA256을 검증한다. 다운로드 중인 .part 파일은 완성된 모델이 아니다. provenance.json과 워커 실행 결과를 확인한다.
- 분리 라이브러리 qwen-packages: qwen-tts 0.1.1, transformers 4.57.3, accelerate 1.12.0, sox 1.5.0, onnxruntime 1.30.0. 기존 voice-env의 torch 2.8 CPU, torchaudio, librosa, soundfile 등을 재사용한다. import는 성공했으나 SoX 실행 파일 없음 경고가 있었다. 실제 합성에 필요한지 확인해야 한다.
- `qwen_voice_worker.py`: JSON stdin/stdout 오프라인 합성 초안. Qwen 모델 다운로드/무결성 확인 완료. 첫 CPU bfloat16/eager 시험이 너무 느려 중단하고 FP32/SDPA로 세 문장 검증 중. x-vector 참고 음성 모드, 텍스트별 WAV 캐시. 아직 실제 합성 검증과 앱 Voice 연결을 완료하지 않았다. `qwen_voice.py` 비동기 호출용 클라이언트 초안도 추가됐으나 앱에는 연결 전이다.
- 집 PC RAM 약 16GB, 작업 당시 여유 약 4GB. C: 여유가 작아 대용량 설치/캐시는 D:를 사용했다. 회사 PC 하드웨어를 새로 확인한다.

## 이어 할 검증

1. 모델 다운로드 완료 및 워커 의존성 오류 확인.
2. 원본의 한 사람 음성 구간으로 서로 다른 한국어 문장 최소 3개 합성. 파일 검색, 결과 안내, 자유 대화를 구분.
3. 기존 로컬 Whisper로 내용 검사하되 작은 ASR의 오류 가능성을 고려하고, 음색 유사성은 별도로 청취 확인. ASR 통과만으로 짱구와 똑같다고 주장하지 않음.
4. 성공 시 비동기 합성/캐시/중단/오류 안내를 앱 Voice에 연결. UI 멈춤과 오래된 응답 뒤늦은 재생 방지. 자동 갱신은 무음 유지.
5. 사용자 설정을 보존해 새 모드를 적용하고 실제 앱에서 검색 요청과 다른 문장 발화를 검증.

## 테스트/배포 후속

사용자가 테스트 ZIP에 문서·검색 데이터·등록 음성을 포함하도록 요청했다. 실제 업무 자료 전체와 샘플 자료 중 범위 질문은 아직 답변 미확인. 개인정보 제외만으로 테스트에 필요한 자료를 누락하지 말 것. 현재 build_share_package.py는 소스/이미지/설명서 중심이고 음성과 개인 DB는 제외하는 구버전 구성이다.

공식 로컬 프로젝트 안내: https://developers.openai.com/es-419/docs/projects?surface=web

## Git 준비 상태

집 PC 로컬 초기 커밋 `4722d36`, 브랜치 master. 사용자가 https://github.com/hwangyksiriai/Computer_jjanggu 업로드를 명시적으로 지시했다. 해당 저장소는 확인 당시 비어 있고 공개 상태다. 소스·이미지·샘플·문서가 대상이며 실제 개인 문서/DB/음성/모델은 업로드하지 않는다. 실제 동기화 완료는 `git remote -v`, `git log -1`과 원격 HEAD로 확인한다.

새 음성 검증: Qwen FP32/SDPA로 “오늘은 어떤 일을 도와줄까?” 문장 일치와 앱 Voice 경로 실제 재생 확인. 인보이스/급여명세서 발음은 작은 Whisper 인식에서 오류가 있어 정확성 미확인. 첫 모델 준비 약 150초, 준비 후 짧은 문장 약 14~16초 측정(PC 상황에 따라 변동). x-vector-only 모드에서 사용하지 않는 참고 음성 코드 encode를 생략하고 speaker embedding을 캐시하는 최적화 후 추가 검증 중. qwen_local 모드를 선택하면 실제 답변을 비동기로 합성하고 요청 취소 시 늦은 재생을 방지한다. 새 모드는 아직 사용자 설정에 활성화 전이다. 46개 테스트 통과.

최신 추가 시험 완료: 검색 시작과 인사 두 문장은 입력대로 인식됐으나 “파일 세 개를 찾았어.”는 반복 인식 오류로 실패. 자유 발화 품질 완료라고 주장하거나 기본 모드를 자동 변경하지 말 것. 두 문장의 성공을 전체 문장 정확성/짱구 음색 보장으로 해석하지 않는다. GitHub 업로드 성공 및 로컬/원격 커밋 일치 확인 완료(최종 커밋은 git log로 확인).

## 2026-09-23 결과 화면 개선 (2026.09.23.2)

사용자가 대화에서 확인한 개선안을 실제 `ResultBrowser`에 적용했다. 중복 숫자·필터 버튼·기술적인 본문 설명을 기본 화면에서 제거하고, 큰 파일 목록과 선택한 문서의 미리보기, 큰 ‘파일 열기’ 버튼을 제공한다. 위치·수정일·검색 근거·분석 집계는 ‘파일 정보’에서 펼친다. 관련 후보가 없으면 해당 영역은 표시하지 않는다.

`document_preview.py`는 PDF 실제 페이지와 EXIF 방향을 반영한 이미지 픽셀을 반환한다. 결과창의 작업 스레드가 읽고 Tk 이미지 생성은 UI 스레드가 담당한다. 늦게 도착한 이전 선택 결과를 버리고, 종료 시 작업 요청을 중단한다. 지원하지 않는 형식은 추출 본문을 보여준다. `pdf_runtime.PDF_LOCK`을 미리보기·썸네일·OCR 렌더링에서 공유한다.

새 검색칸은 원래 검색어를 보여주며 앱의 전체 검색으로 연결한다. 사진 검색은 기존 사진 모음으로 넘긴다. 추가 색인 시 보고 있는 파일이 표시 범위 밖으로 밀려도 선택을 보존한다. 수정일을 급여 지급월로 표시하지 않는다. 검색 범위와 미분석 상태는 짧은 안내로 유지한다.

회귀 검증: `test_document_preview.py`, `test_pdf_runtime.py`, `test_result_browser.py`, 기존 문서 검색·한글 입력 검사. `verify_easy.py`의 실행 파일 자체 검사에 실제 2쪽 PDF 렌더링·페이지 전환·원본 미실행 검증을 추가했다. 로컬 실행판은 기존 바탕화면 바로가기 대상을 갱신하며 개인 문서·DB·목소리 설정은 유지한다. 이 변경은 아직 GitHub 릴리스로 게시하지 않았다.
