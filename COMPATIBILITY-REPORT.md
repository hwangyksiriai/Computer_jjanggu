# 현재 PC 호환성 검증 · 2026-09-18

## 이번 검증 결과

- 집중 모드 후속 수정: 실제 설정에서 수동 집중이 켜져 있어 요청 반응이 억제되는 원인을 확인. 직접 입력한 요청은 처리 중 및 완료 직후 집중 모드의 음성·동작 억제에서 예외로 처리하고, 주변 활동에는 집중 모드를 유지. 음소거와 애니메이션 끄기 설정은 여전히 존중. 수동 집중을 켠 상태로 지원 요청별 음성 호출과 음소거 무음을 재검증함.

- 요청별 반응: 검색·대화·정리·꾸미기·춤·트레이·방·앱 목록에 접수/완료 음성 및 동작 연결. `verify_bubble.py`에서 지원 요청별 두 단계 음성 호출, 음소거 시 무음, 손 위치를 따라가는 돋보기 렌더링 통과. 음소거·집중 모드·움직임 끄기 설정은 유지함. 접수 음성이 끝난 뒤 완료 음성을 재생하며 새 요청은 이전 대기 음성을 취소함.

- 검색 피드백 추가 검증: 지연된 검색 중 전송 확인·입력칸 비우기·재전송 비활성화·캐릭터 움직임·경과 시간, 성공/빈 결과/실패 시 동작 종료 및 버튼 복구 통과 (`verify_bubble.py`).
- 원본 음성 재생 오류 수정: UTF-8로 전달한 한글 파일 경로를 Windows 기본 인코딩으로 읽어 파일을 못 찾는 오류를 재현. 재생기 입력을 명시적 UTF-8로 변경. 사용자가 등록한 실제 WAV를 음량 0으로 python.exe/pythonw.exe에서 모두 재생하여 종료 코드 0 확인. 등록 경로는 변경하지 않음.

- 단위·회귀 테스트 33개 통과: 기존 27개 + 호환성 6개.
- UTF-8 BOM / UTF-16 / CP949 본문, 한글·공백 경로 통과.
- DOCX / PPTX / XLSX XML 추출 변형 통과. XLSX inlineStr 셀을 읽지 못하던 문제 수정. Office 앱의 모든 서식·수식 계산 호환성을 뜻하지 않음.
- 텍스트 PDF 추출, 암호 PDF 안내, 손상 문서 처리 통과.
- Windows 이미지 OCR, 한국어 TTS WAV(4.91초) 통과.
- 8개 UI 화면과 TkDND 등록, Tcl 다중 파일 경로(한글·공백·중괄호) 파싱 통과.
- 방의 침대·책상·장난감·서랍 가운데를 클릭하는 실제 Tk 이벤트 4개 통과. 기존 빈 사각형의 내부가 클릭되지 않을 수 있는 문제 수정.
- 실제 연결된 모니터 2대의 음수/양수 좌표 왕복 통과.
- 마이크: Windows 기본 입력 장치 없음. Realtek 입력은 기존 16kHz 미지원. 마이크 선택 UI 및 44.1/48kHz 입력→16kHz 변환 추가. 장치 설정 검사와 합성 파형 변환 통과. 마이크 녹음은 수행하지 않음.
- 앱 바로가기 목록: 바탕화면 하위 전체 탐색을 제거. 현재 PC 120개, 약 0.02초.

## 아직 확인하지 못한 범위

후속 추가: `verify_bubble.py` 통과. 주머니 창을 숨긴 상태에서 캐릭터 클릭→말풍선→본문 검색→파일 열기→후속 필터→춤 실행, 두 실제 모니터 안 배치, 드래그 구분, 닫은 뒤 늦은 응답 안전성을 확인함. 외부 스크린샷 기반 시각 검증과는 별도임.

- Explorer↔캐릭터 및 다른 외부 앱 간 실제 마우스 드래그 전송. Windows 화면 도구가 두 번 모두 `SetIsBorderRequired 0x80004002` 오류를 반환했고 Tk 접근성 트리도 없어 좌표를 추측한 조작은 수행하지 않음. 등록·파싱 성공과 실제 외부 드래그 성공은 다름.
- 실제 사용자 발화와 마이크 신호 품질. AI와 생활 설정 → 마이크 선택에서 실제 마이크를 선택한 뒤 음성 입력 버튼으로 확인해야 함.
- 다른 PC/Windows 버전, 혼합 DPI, 모니터 물리 연결 해제, 장기 대량 문서 검색 정확도.
- 파일 외부 전송 없음은 앱의 오프라인 추론 코드·설정 기준. OS 및 타 프로그램까지 네트워크 패킷 감사를 한 것은 아님.

실행: `python -m unittest test_core test_monitors test_enhanced test_compatibility -q`, `python smoke_enhanced.py`, `python verify_integrations.py`, `python verify_monitors.py`.
