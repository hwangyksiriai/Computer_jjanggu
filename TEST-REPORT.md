# 확장 기능 검증 — 2026-09-18

- 단위/회귀 테스트: **27개 통과**. 이동 충돌·복원·범위 제한·듀얼 모니터·분류 학습 규칙·메타데이터 유지·보상 중복 방지 포함.
- 확장 UI 스모크: **8개 화면 통과**. 파일 드롭 등록, 꾸미기·장식·프레임 애니메이션 초기화 확인.
- 앱 통합 흐름: **통과**. 실제 로컬 AI 색인→비동기 검색→결과 표시→다중선택 트레이→꾸미기·방 화면.
- 실제 모니터: 왼쪽 `(-1920, 0, 0, 1080)`, 오른쪽 `(0, 0, 1920, 1080)` 좌표 왕복 검증.
- 로컬 OCR: 이름이 `scan_0032.png`인 합성 청구서 글자 인식·인보이스 분류 통과. 스캔 PDF도 검색 대상에 포함됨.
- 음성 합성: 한국어 WAV 생성 확인.
- 음성 입력: 실제 사용자 마이크를 녹음하지 않고 합성 문장으로 검증. `인보이스 찾아줘` → Whisper 결과 `인 보이스 찾아줘`. 앱은 이 띄어쓰기를 정규화함.
- AI 의도 분류: `정리 좀 도와줘` → CLEAN.
- AI 근거 답변: 제공한 청구서에 대해 `청구 금액은 USD 1320이며, 지급기한은 2026년 10월 1일입니다.` 반환.
- 패키지 감사: Transformers.js 4.3.0 설치 기준 npm 감사 취약점 0개.

## 소규모 검색 수용 테스트

자료는 직접 만든 텍스트·표지 이미지·스캔 청구서·스캔 PDF 총 9개입니다. 개인 파일은 테스트에 사용하지 않았습니다.

| 질문 | 필요한 파일의 위치 |
|---|---|
| 인보이스 찾아줘 | 필요한 3개가 상위 3개에 모두 포함 |
| 용역 계약 기간이 적힌 문서 | 1위 |
| 바닷가 여행 숙소 예약 계획 | 1위 |
| 회의에서 일정 미룬 내용 | 1위 |
| 파란 표지 제안서 | 파란 표지 1위 |
| 빨간 표지 제안서 | 빨간 표지 1위 |

이 테스트의 평균 Recall@3은 1.0이지만, 9개 합성 자료로 구성된 작은 수용 테스트의 결과입니다. 실제 사용자의 전체 문서에 대한 정확도나 정밀도 100%를 뜻하지 않습니다. 관련 후보에는 다른 파일도 나타납니다.
모델이 이미 준비된 검색은 이 테스트에서 약 0.14–0.32초였으며 초기 로딩과 색인·대화 시간은 별도입니다.

## 아직 실제 사용으로 확인해야 하는 부분

- 장기 사용성, 다양한 문서·이미지의 검색 품질, 서로 다른 마이크와 외부 앱의 드롭 호환성.
- Windows 전체화면/회의 감지는 창 크기·제목 기반 휴리스틱입니다.
- Windows 화면 캡처 도구의 `SetIsBorderRequired` 오류로 외부 스크린샷 기반 시각 검증은 수행하지 못했습니다. 위 UI 검증은 위젯과 실제 앱 콜백 기반입니다.
- 배경 변경·로그인 시작·실제 바탕화면 이동은 사용자가 앱에서 선택할 때 수행합니다. 자동 테스트에서 이러한 개인 설정을 변경하지 않았습니다.

원본 측정 결과: `.local/ai-acceptance/report.json`. 검색 횟수·평균 시간·사용자 피드백은 로컬 앱 설정 화면에서 확인할 수 있습니다.
