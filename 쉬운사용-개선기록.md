# 사용자가 배우지 않아도 되는 정리 앱

이 평가는 요청한 사용자 관점에서 설계자가 검토한 내용입니다. 실제 청소년·중장년 사용자 인터뷰나 사용성 실험을 했다는 뜻은 아닙니다. 연령보다 컴퓨터 경험과 정리 습관을 기준으로 판단했습니다.

## 따끔한 피드백

- 컴퓨터에 익숙하지 않은 사용자: “Python, PATH가 뭔지 모르는데 설치부터 공부하라고요?” 설치 설명을 길게 쓰는 것으로 해결되지 않습니다. 실행 환경을 앱에 포함했습니다.
- 글씨와 메뉴가 부담스러운 사용자: “내 파일 찾으러 왔는데 학습, 트레이, AI 미분석이 왜 나와요?” 기본 메뉴를 네 개로 줄이고, 검색창과 결과 이름을 크게 했습니다. 결과에서는 열기와 미리보기만 보여 줍니다.
- 정리를 미루는 사용자: “꾸미기 전에 정리부터 끝내고 싶어요.” 정리 화면을 위치 확인 → 목록 확인 → 정리로 구성했습니다. 꾸미기는 더보기로 옮겼습니다.
- 파일이 사라질까 걱정하는 사용자: “주머니에 넣는다는 게 삭제한다는 뜻인가요?” 보관 폴더로 이동한다고 직접 설명하고, 경로·취소·되돌리기 버튼을 보여 줍니다.
- 검색하는 사용자: “커피라고 썼는데 왜 AI 대화 설치하래요?” 메인 검색창은 입력 단어를 검색으로 처리합니다. 대화 의도 판별을 거치지 않습니다.

## 구현

- 기본 실행 화면: EasyApp. 고급 기능은 기존 EnhancedApp을 재사용하며 더보기로 이동.
- Windows 64비트 간편판: Python, Tk, 필요한 라이브러리를 함께 묶은 폴더형 실행 파일. ZIP 전체 압축 해제 후 exe 실행.
- Windows 시작 시 실행 설정은 패키징된 exe 경로도 지원.
- 파일 정리 전 미리보기와 명시적 실행, 취소, 정리 후 완료 안내, 마지막 정리 복원.
- 자동 정리는 기본으로 켜지 않음. 기존 설정의 선택은 보존.
- AI 모델이 없는 상태도 정상적인 기본 사용 상태로 안내.

## 검증과 남은 일

- 기존 파일 처리·모니터·호환성·백그라운드·검색 필터 43개 회귀 검사.
- 임시 파일만 사용하는 실제 Tk 화면 검사: 단어 검색, 화면 이동, 취소 시 원본 유지, 정리 실행, 원본 내용 그대로 복원.
- 배포 exe에서도 같은 검사를 수행한 결과는 배포 검증 기록에 보관.
- 간편판에는 AI 대화·음성 검색 모델을 넣지 않았습니다. 모델 설치 버튼도 노출하지 않습니다.
- 아직 서명된 설치 프로그램이나 자동 업데이트가 아닙니다. ZIP 압축 해제는 필요하며 회사 보안 정책에 따라 실행이 차단될 수 있습니다.
- 첫 화면에 큰 기능 버튼만 남겼어도 고급 설정과 미리보기 일부에는 기존 UI가 남아 있습니다.
- 실제 대상 사용자에게 설명 없이 시작·검색·정리·복원을 시켜 보고 관찰하는 사용성 검증은 아직 하지 않았습니다. 실제 100%·150% 배율과 다양한 화면에서의 육안 검증도 후속 과제입니다.

## 개발자 빌드

Python 3.12 Windows 환경에서 requirements.txt와 pyinstaller를 설치한 뒤:

```powershell
python -m unittest test_core test_monitors test_enhanced test_compatibility test_background test_refinement -q
python verify_easy.py
python build_windows.py dist
```

build_windows.py는 개인 설정·문서·음성·검색 DB를 복사하지 않습니다. 실행 시 생긴 데이터를 재배포 ZIP에 넣지 마세요.
